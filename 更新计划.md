# 恶搞启动器生成工具 v3.0 升级计划 (Update Plan)

经过对当前代码库 (`v2.0`) 的深入审查，我发现之前提出的一些改进点**已经在最新代码中被部分实现**，但仍有巨大的优化空间。

---

## ✅ 已完成任务（v3.0 最高优先级）

> 以下任务已在 2026-04-07 完成并通过验证。

### 1. 多线程并发扫描引擎 🔴高优先级
- [x] 重写 `scan_drives()` — 使用 `ThreadPoolExecutor`，每个驱动器一个 worker 线程
- [x] 重写 `scan_common_paths()` — 所有常用目录并发扫描，`max_workers` 上限 8
- [x] 实现 `threading.Event` — "一处发现，全盘停止"，线程间高效通信
- [x] 异常压制增强 — `PermissionError`、`OSError` 精确捕获，系统目录不再导致崩溃

### 2. 替换 BAT 自更新机制 🔴高优先级
- [x] 移除全部 `.bat` 脚本释放和 `subprocess.Popen` 调用
- [x] 实现 `atexit` + `os.replace` 原子替换方案
- [x] 实现 `MoveFileExW` (`MOVEFILE_DELAY_UNTIL_REBOOT`) 系统级延迟替换作为 fallback

### 3. 代码质量统一 🔴高优先级
- [x] `file_inflator.py` 中文变量名全部改为英文（`膨胀倍数` → `inflate_ratio` 等 6 处）
- [x] `launcher_core.py` 补充完整类型注解（`typing` 模块）
- [x] 清理所有裸 `except:` 为精确异常类型
- [x] `builder_gui.py` 合并模板补充新增 import（`atexit`、`concurrent.futures`、`json`、`datetime`、`typing`）

---

## 🔲 后续计划

### 🟡 中优先级任务

#### 任务 1：恶搞模板库 ✅
- **文件**: `builder/widgets/config_panel.py`
- **工作量**: 低
- **内容**:
  - [x] 在 Builder 主界面顶部添加下拉菜单（ComboBox）
  - [x] 提供常见恶搞目标预设（原神、崩铁、英雄联盟、微信、Steam）
  - [x] 选择后自动填入目标进程名、网址等配置项

#### 任务 2：增强伪装加载界面 ✅
- **文件**: `template/fake_ui.py`
- **工作量**: 低
- **内容**:
  - [x] 添加滚动"加载任务列表"（正在校验资源完整性、正在连接服务器…）
  - [x] 状态文本随机化，增强真实感
  - [x] 支持在 Builder 中上传并注入启动背景图（Base64 Splash Image）

#### 任务 3：Builder UI 组件抽离 ✅
- **文件**: `builder/widgets/` [新目录]
- **工作量**: 中
- **内容**:
  - [x] 抽取 `config_panel.py` — 配置面板
  - [x] 抽取 `log_viewer.py` — 日志查看器
  - [x] 将打包核心逻辑从 GUI 分离到 `builder/builder_core.py`
  - [x] 添加 `history_manager.py` — 配置自动保存/加载

#### 任务 4：扫描器模块化 ✅
- **文件**: `template/scanner/` [新目录]
- **工作量**: 中
- **内容**:
  - [x] 创建 `base_scanner.py` — 抽象基类与公用工具
  - [x] 拆分 `drive_scanner.py` — 驱动器扫描（含常用路径策略 & 全盘策略）
  - [x] 拆分 `registry_scanner.py` — 注册表扫描
  - [x] 拆分 `cache_scanner.py` — 缓存扫描 + 自身嵌入数据
  - [x] GameScanner 重构为编排层，按优先级调度子扫描器

#### 任务 5：添加单元测试 ✅
- **文件**: `tests/` [新目录]
- **工作量**: 高
- **内容**:
  - [x] 引入 `pytest` 测试框架
  - [x] 覆盖核心逻辑：文件膨胀（`FileInflator`）— 含 parse_size bug 修复
  - [x] 覆盖核心逻辑：配置生成（`HistoryManager`）
  - [x] 覆盖核心逻辑：扫描匹配（`BaseScanner`、`CacheScanner`、`DriveScanner`）

---

### 🟢 低优先级任务

#### 任务 6：代码混淆与签名
- **工作量**: 中
- **内容**:
  - [ ] 打包前对 `boot.py` 和 `launcher_core.py` 进行轻量级混淆（变量名替换、插入无意义代码）
  - [ ] 添加 Authenticode 签名（SignTool），降低 SmartScreen 拦截

#### 任务 7：代码规范补充
- **工作量**: 低
- **内容**:
  - [ ] 提取硬编码字符串（错误消息、日志格式等）到 `constants.py`
  - [ ] 集成 `ruff` 或 `pylint` 进行代码规范检查
  - [ ] 集成 `mypy` 进行类型检查
  - [ ] `requirements.txt` → `requirements.lock` 锁定精确版本

#### 任务 8：UI 体验增强 ✅
- **工作量**: 中
- **内容**:
  - [x] 添加深色/浅色模式切换
  - [x] 生成前预览 — 打包前预查看 Launcher 界面效果
  - [x] 历史记录 — 记录最近生成的配置，支持快速复用
  - [x] 内置日志查看器 — UI 内直接查看日志，无需打开文件

#### 任务 9：性能优化
- **工作量**: 中
- **内容**:
  - [ ] 扫描结果缓存 — 写入 `%APPDATA%/cache.json`，支持增量更新
  - [ ] 启动速度优化 — 探索 `python_portable` 模式替代 PyInstaller 单文件打包

#### 任务 10：国际化支持
- **工作量**: 高
- **内容**:
  - [ ] 将硬编码中文字符串提取到 `locale/` 目录
  - [ ] 实现多语言切换（中文/英文）

#### 任务 11：统一错误处理
- **工作量**: 低
- **内容**:
  - [ ] 使用 `builder/utils/errors.py` 中的自定义异常体系
  - [ ] 所有异常增加日志记录
  - [ ] 对 `PermissionError`、`AccessDenied` 等系统级异常静默处理

---

## 落地优先级总览

| 优先级 | 任务 | 工作量 | 收益 | 状态 |
|--------|------|--------|------|------|
| 🔴 高 | 多线程扫描 | 中 | 显著提升扫描速度 | ✅ 已完成 |
| 🔴 高 | BAT 脚本替换 | 高 | 提升免杀能力 | ✅ 已完成 |
| 🔴 高 | 代码质量统一 | 低 | 提升可维护性 | ✅ 已完成 |
| 🟡 中 | 恶搞模板库 | 低 | 提升易用性 | ✅ 已完成 |
| 🟡 中 | 增强伪装 UI | 低 | 提升真实感 | ✅ 已完成 |
| 🟡 中 | UI 组件抽离 | 中 | 提升可维护性 | ✅ 已完成 |
| 🟡 中 | 扫描器模块化 | 中 | 提升可扩展性 | ✅ 已完成 |
| 🟡 中 | 单元测试 | 高 | 保障重构安全 | ✅ 已完成 |
| 🟢 低 | 代码混淆与签名 | 中 | 降低杀软报警 | 🔲 待实现 |
| 🟢 低 | 代码规范补充 | 低 | 自动化检查 | 🔲 待实现 |
| 🟢 低 | UI 体验增强 | 中 | 专业工具感 | ✅ 已完成 |
| 🟢 低 | 性能优化 | 中 | 二次启动更快 | 🔲 待实现 |
| 🟢 低 | 国际化支持 | 高 | 扩大用户群 | 🔲 待实现 |
| 🟢 低 | 统一错误处理 | 低 | 更易调试 | 🔲 待实现 |

---

*本文档由 AI 审查代码后自动生成，用于指导下一步的迭代开发。*
*最后更新: 2026-04-07*