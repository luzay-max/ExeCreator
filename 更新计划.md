# 恶搞启动器生成工具 v3.0 升级计划 (Update Plan)

经过对当前代码库 (`v2.0`) 的深入审查，我发现之前提出的一些改进点**已经在最新代码中被部分实现**，但仍有巨大的优化空间。

## 一、 已实现与待改进功能对比

### 1. 启动器隐蔽性与真实感
- ✅ **已实现**: 
  - `fake_ui.py` 中的进度条已经加入了 `random.uniform` 来模拟非线性增长。
  - `builder_gui.py` 已经包含了“元数据伪装 (高级)”功能（可以伪造公司名称、文件描述、版本号等）。
- ❌ **待改进 (v3.0 目标)**:
  - 加载界面的状态文本（"Initializing..."）依然是固定的，缺乏真实感。
  - 缺乏免杀/防误报机制，`SelfUpdater` 释放的 `.bat` 脚本特征明显，极易被杀软拦截。
  - 不支持自定义 Splash Screen（启动图），UI 仍显单调。

### 2. 扫描算法性能
- ✅ **已实现**:
  - `launcher_core.py` 已经具备了优先级扫描逻辑：`本地缓存` -> `注册表` -> `常用路径(Program Files等)` -> `全盘扫描`。
  - 在全盘扫描时，加入了黑名单过滤 (`blacklist_dirs`)，并且在 AppData 目录中加入了深度限制（`depth > 4` 时停止递归），极大提升了效率。
- ❌ **待改进 (v3.0 目标)**:
  - 依然是**单线程**的同步阻塞式目录遍历 (`os.walk`)。
  - 对于多块硬盘（C、D、E盘），目前是串行扫描，速度瓶颈明显。需要引入多线程并发扫描机制。

### 3. 生成器 (Builder) 体验
- ❌ **待改进 (v3.0 目标)**:
  - 缺少“预设恶搞模板”（如一键选择原神、英雄联盟等），目前仍需用户手动输入目标进程名和网址。
  - 异常处理可以更优雅，全盘扫描时偶尔出现的 `PermissionError` 需要被更安静地处理。

---

## 二、 v3.0 版本开发路线图

### 阶段一：UI 与用户体验升级 (Builder & Fake UI)
- [ ] **增强伪装加载界面** (`template/fake_ui.py`)：
  - 添加一个滚动的“加载任务列表”（如：正在校验资源完整性、正在连接服务器...）。
  - 支持在 Builder 中上传并注入一张启动背景图 (Splash Image)。
- [ ] **添加恶搞模板库** (`builder/builder_gui.py`)：
  - 在主界面顶部添加一个下拉菜单（ComboBox），提供常见的恶搞目标快捷填表功能。

### 阶段二：扫描引擎重构 (Launcher Core)
- [ ] **引入多线程并发扫描** (`template/launcher_core.py`)：
  - 重写 `scan_drives()` 方法，使用 `concurrent.futures.ThreadPoolExecutor` 对系统中的多个驱动器或根目录并发执行 `os.walk`。
  - 使用线程安全的队列或事件 (`threading.Event`) 来传递 `found_path` 并通知其他线程提前终止，实现“一处发现，全盘停止”。
- [ ] **异常压制与健壮性**：
  - 在扫描核心中增加更严密的 `try...except` 块，确保遇到系统级权限目录时不会导致整个扫描器崩溃或卡顿。

### 阶段三：反侦察与免杀增强 (Security)
- [ ] **重构自更新逻辑** (`template/launcher_core.py`)：
  - 废弃当前写入 `.bat` 脚本并用 `subprocess` 调用的粗暴方式。
  - 探索使用更隐蔽的 Python 内存注入方式，或者使用 `os.rename` 配合退出钩子 (`atexit`) 来完成自身缓存的更新，降低杀毒软件的启发式报警率。
- [ ] **代码混淆**：
  - 在 Builder 打包阶段（调用 PyInstaller 前），对 `boot.py` 和 `launcher_core.py` 的源码进行轻量级混淆（如变量名替换、插入无意义代码），破坏静态特征码。

---

## 三、代码质量与工程化改进

### 3.1 代码规范优化
- [ ] **统一变量命名** (`builder/utils/file_inflator.py:48`)：
  - 将 `self.膨胀倍数` 改为 `self.inflate_ratio`
  - 全面使用英文命名，移除中文变量名
- [ ] **添加类型提示**：
  - 在 `builder_gui.py`、`launcher_core.py` 等核心模块添加 `typing` 注解
  - 便于静态分析工具（mypy）检查
- [ ] **提取魔法字符串**：
  - 将硬编码的字符串（错误消息、日志格式等）提取到 `constants.py`

### 3.2 工程化建设
- [ ] **单元测试**：
  - 添加 `pytest` 测试框架
  - 至少覆盖核心逻辑：文件膨胀、配置生成、扫描匹配
- [ ] **静态代码检查**：
  - 集成 `ruff` 或 `pylint` 进行代码规范检查
  - 集成 `mypy` 进行类型检查
- [ ] **依赖锁定**：
  - `requirements.txt` → `requirements.lock` 锁定精确版本

---

## 四、架构优化

### 4.1 Builder 重构
```
builder/
├── builder_gui.py          # 主界面 (臃肿，1200+ 行)
+├── widgets/               # 抽取 UI 组件
+│   ├── config_panel.py   # 配置面板
+│   ├── progress_dialog.py # 进度对话框
+│   └── log_viewer.py      # 日志查看器
+└── builder_core.py       # 打包核心逻辑 (从 gui 分离)
```

### 4.2 Launcher 扫描器重构
```
template/
├── launcher_core.py        # 扫描逻辑 (~500 行)
+├── scanner/               # 扫描器模块化
+│   ├── drive_scanner.py   # 驱动器扫描
+│   ├── registry_scanner.py # 注册表扫描
+│   └── cache_scanner.py   # 缓存扫描
+└── strategy/              # 扫描策略模式
+    ├── base.py
+    ├── priority_scan.py   # 优先级扫描
+    └── full_scan.py       # 全盘扫描
```

---

## 五、用户体验提升

### 5.1 界面主题
- [ ] 添加深色/浅色模式切换

### 5.2 预览与历史
- [ ] **生成前预览**：在打包前预览 Launcher 界面效果
- [ ] **历史记录**：记录最近生成的配置，支持快速复用
- [ ] **内置日志查看器**：在 UI 内直接查看日志，无需打开文件

### 5.3 国际化
- [ ] 将硬编码中文字符串提取到 `locale/` 目录
- [ ] 支持多语言切换

---

## 六、性能优化

| 优化点 | 当前实现 | v3.0 目标 |
|--------|----------|-----------|
| **多线程扫描** | 单线程 `os.walk` | `ThreadPoolExecutor` 并发扫描 |
| **目录缓存** | 无 | 扫描结果写入 `%APPDATA%\cache.json`，增量更新 |
| **启动速度** | PyInstaller 单文件较慢 | 考虑 `python_portable` 模式 |

---

## 七、错误处理增强

### 7.1 当前问题
```python
# 当前：错误直接抛出，日志缺失
def get_file_size(self, file_path: str) -> int:
    if not os.path.exists(file_path):
        raise FileNotFoundError(f"文件不存在: {file_path}")
```

### 7.2 改进方案
- [ ] 统一错误处理：使用 `builder/utils/errors.py` 中的自定义异常
- [ ] 所有异常增加日志记录
- [ ] 对 `PermissionError`、`AccessDenied` 等系统级异常静默处理

---

## 八、安全与反杀增强（补充）

### 8.1 当前实现问题
| 特性 | 问题 | 影响 |
|------|------|------|
| BAT 脚本自更新 | 极易被杀软拦截 | 特征明显，误报率高 |
| 无代码混淆 | 静态特征明显 | 容易被杀软识别 |
| 无签名 | 分发可信度低 | Windows SmartScreen 拦截 |

### 8.2 改进方案
- [ ] **替换 BAT 脚本**：使用 `os.rename` + `atexit` 钩子替代 BAT
- [ ] **代码混淆**：打包前对 `boot.py` 和 `launcher_core.py` 进行变量名混淆
- [ ] **代码签名**：添加 Authenticode 签名（SignTool）

---

## 九、落地优先级

| 优先级 | 改进项 | 工作量 | 收益 |
|--------|--------|--------|------|
| 🔴 高 | 多线程扫描 | 中 | 显著提升扫描速度 |
| 🔴 高 | BAT脚本替换 | 高 | 提升免杀能力 |
| 🔴 高 | 代码质量统一 | 低 | 提升可维护性 |
| 🟡 中 | 抽取 UI 组件 | 中 | 提升可维护性 |
| 🟡 中 | 添加单元测试 | 高 | 保障重构安全 |
| 🟢 低 | 国际化支持 | 高 | 扩大用户群 |

---
*本文档由 AI 审查代码后自动生成，用于指导下一步的迭代开发。*